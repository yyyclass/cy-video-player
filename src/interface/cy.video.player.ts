export interface CyVideoPlayerOptions {
    /**
     * video address
     *
     * 视频地址
     */
    src?: string;
    /**
     * If this property is present, the video is played immediately after it is ready.
     *
     * 如果出现该属性，则视频在就绪后马上播放
     */
    autoplay?: boolean;
}

